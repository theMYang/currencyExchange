<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Configuration 3.0//EN" "mybatis-3-mapper.dtd" >

<mapper namespace="com.wbo.currencyExchange.dao.dealDao.DealDao">
	
	<insert id="insertDeal">
		insert into deal_table (buy_order_id, sell_order_id, deal_amount, deal_price) values
			(#{buyOrderId}, #{sellOrderId}, #{dealAmount}, #{dealPrice})
	</insert>

	
</mapper>